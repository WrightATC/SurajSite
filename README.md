# Valentine Page (GitHub Pages)

## Deploy
1. Create a new GitHub repo (public).
2. Upload `index.html`, `style.css`, `script.js`.
3. In GitHub: Settings → Pages
   - Source: Deploy from a branch
   - Branch: `main` / root
4. Visit the Pages URL GitHub gives you.

## Customize
- Change the name in `index.html` (the title line).
- Change colors in `style.css` under :root.